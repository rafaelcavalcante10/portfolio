import { DatePipeFormatPipe } from './date-pipe-format.pipe';

describe('DatePipeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
